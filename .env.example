# Server-only AssemblyAI credential. Never expose this to the browser.
ASSEMBLYAI_API_KEY=

# Include only if required by the selected official Voice Agent configuration.
ASSEMBLYAI_AGENT_ID=

# Optional local development mode. Must be false for the real hackathon demo.
NEXT_PUBLIC_DEMO_MODE=false
